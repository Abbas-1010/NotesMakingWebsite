<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: 'Georgia', serif;
            background: linear-gradient(to bottom, #2e2b26, #1c1a18);
            margin: 0;
            padding: 2rem;
            background-image: 
                repeating-linear-gradient(0deg, transparent, transparent 30px, rgba(255,255,255,0.02) 31px),
                linear-gradient(to bottom, #3b342e 0%, #2a2622 100%);
            color: #f5f3e7;
        }
    
        button, input[type="submit"] {
            background: linear-gradient(to bottom, #5c4a3c, #3f332b);
            border: 2px dashed #c1a57d;
            padding: 10px 20px;
            font-weight: bold;
            font-family: 'Georgia', serif;
            cursor: pointer;
            box-shadow: 2px 2px 4px rgba(0,0,0,0.4);
            border-radius: 6px;
            color: #fdfaf5;
            margin: 0.5rem;
        }
    
        a {
            text-decoration: none;
            color: #dcb980;
        }
    
        h1, h2, h3 {
            font-family: 'Georgia', serif;
            border-bottom: 2px dashed #b49771;
            padding-bottom: 5px;
            background: linear-gradient(to right, #4c4035, #3a3128);
            display: inline-block;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            margin: 1rem 0;
            color: #f5e9d6;
        }
    
        label {
            font-weight: bold;
            font-size: 1.1rem;
            color: #e4d3b2;
        }
    
        input[type="text"],
        input[type="url"],
        textarea {
            width: 100%;
            max-width: 600px;
            padding: 0.5rem;
            margin-top: 0.3rem;
            margin-bottom: 1rem;
            border: 1px solid #9e8065;
            background: linear-gradient(to bottom right, #4a4038, #3b322b);
            border-radius: 5px;
            font-family: 'Georgia', serif;
            color: #f5f3e7;
        }
    
        form {
            background: linear-gradient(to bottom right, #3f382f, #2e2822);
            border: 2px dashed #a38660;
            padding: 2rem;
            border-radius: 15px;
            max-width: 700px;
            margin: 2rem auto;
            box-shadow: 5px 5px 15px rgba(0,0,0,0.3);
        }
    
        div {
            background: linear-gradient(to top left, #3c352c, #2c2620);
            border: 2px dashed #8c7255;
            padding: 2rem;
            margin-top: 2rem;
            border-radius: 20px;
            box-shadow: 4px 4px 12px rgba(0,0,0,0.3);
        }
    
        p {
            background: #4b3e32;
            padding: 1rem;
            border-left: 4px solid #c2a06c;
            margin-top: 0.5rem;
            border-radius: 4px;
            color: #f4efe7;
        }
    </style>
    
</head>
<body>
    <form method="get" action="/upNote">
        <input type="hidden" name="oldt" value="<%=dab.bookTitle%>"/>
        <input type="text" name="booktitle" value="<%=dab.bookTitle%>"/>
        <textarea name="brief"><%=dab.brief%></textarea>
        <input type="url" name="linkofbook" value="<%= dab.linkofbook %>"/>
        <textarea name="notes"><%= dab.notes %></textarea>
        <input type="submit" value="Update"/>
    </form>
</body>
</html>